

{{#each comments as |comment| }}
<div class="container mx-auto mt-8 p-4 bg-white shadow-lg">
    <p>{{comment.body}}</p>
    <p class="text-gray-600">
        <span class="text-xs font-semibold">Commented by {{comment.user.name}}</span>
    </p>

</div>
{{/each}}